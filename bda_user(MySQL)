-- DROP TABLE IF EXISTS bda_user; #초기화 트리거. 쓸거면 각주표시 지우면 됨
CREATE TABLE bda_user (
  user_index BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_name VARCHAR(30) NOT NULL,
  phone VARCHAR(13),
  email VARCHAR(50),
  id VARCHAR(16) NOT NULL UNIQUE,
  pw VARCHAR(100) NOT NULL
);

